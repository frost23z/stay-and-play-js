window.__ENV__ = {
    // Empty string = same origin the page was loaded from.
    // Only hardcode a value here if the API is genuinely served from a
    // different host/port than the page itself.
    API_BASE_URL: '',
}
